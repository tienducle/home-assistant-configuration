- platform: template
  switches:
    snack:
      friendly_name: 'Snack'
      value_template: '{{ states("switch.snack") }}'
      turn_on:
        service: rest_command.feeder_feed
        data_template:
          feedamount: "{{ states('input_number.feedamount') | int }}"
      turn_off:
